
// A simple Star class
/*
let Star = function() {
  this.bright_prob=0.8;
  this.dim_prob=0.2;
  this.position = createVector(random(-2000,2000), random(-2000,2000));
  this.color=200;
  var r=random();
  if(r<this.dim_prob){
    this.color=150;
  }
  if(r>this.bright_prob){
    this.color=255;
  }
};
*/
/*Star.prototype.run = function() {
  this.update();
  this.display();
};

// Method to update position
Star.prototype.update = function(){
  
};*/

// Method to display
/*
Star.prototype.display = function() {  
  stroke(this.color);
  strokeWeight(2);
  fill(this.color);
  ellipse(this.position.x, this.position.y, 1, 1);
};
*/
